// import vectorDB from '../utils/vectorDB';

// export const addPDF = async (pdfPath: string): Promise<void> => {
//     await vectorDB.addPDFToDatabase(pdfPath);
// };

// export const generateTestFromPDF = async (query: string, numQuestions: number): Promise<string[]> => {
//     const results = await vectorDB.queryDatabase(query);
//     return results.slice(0, numQuestions);
// };
